@*@model List<ContactLib.Contact>*@
@model List<ContactLibSQL.Contact>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Contacts</h2>

<p>
    @Html.ActionLink("New contact", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Surname
        </th>
        <th>
            PhoneNumber
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        { Html.RenderPartial("Partial", item); }
    }

</table>
